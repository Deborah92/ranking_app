<small>Owned by <%= @dog.user.email %> </small>
<h2><%= @dog.name %></h2>
<% if user_signed_in? %>
    <%= link_to "Edit Dog", edit_user_dog_path(current_user, @dog) %>
    <%= button_to 'Delete Dog', @dog, method: :delete, data: {confirm: 'Are you sure you want to delete this dog'} %>
<% end %>